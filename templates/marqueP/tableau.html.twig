{% extends 'base.html.twig' %}

{% block title %}Liste des URL{% endblock %}

{% block body %}
    <div class="container">
        <h1>Liste des URL</h1>

        {% if marquesP is not empty %}
            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>URL</th>
                        <th>Année de création</th>
                        <th>Commentaire</th>
                    </tr>
                </thead>
                <tbody>
                    {% for mpr in marquesP %}
                        <tr>
                            <td>{{ mpr.id }}</td>
                            <td>{{ mpr.url }}</td>
                            <td>{{ mpr.dateCreation | date("d/m/Y") }}</td>
                            <td>{{ mpr.commentaire }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p>Aucune URL trouvé.</p>
        {% endif %}
    </div>
{% endblock %} 